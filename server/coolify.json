{
    "build_pack": "nixpacks",
    "base_directory": "/server",
    "publish_directory": "/",
    "port_exposes": "3001",
    "settings": {
        "is_static": false
    },
    "commands": {
        "build": "npm install && npm run build",
        "start": "node server.js"
    }
}
